# slf4j-simple config. Default to WARN so the Azure SDK's per-request INFO chatter — notably
# azure-identity's "Returning token from cached credential ..." on every model call — does not stream to
# stderr and corrupt the full-screen Lanterna TUI. WARN and ERROR still surface (startup/auth failures).
org.slf4j.simpleLogger.defaultLogLevel=warn
